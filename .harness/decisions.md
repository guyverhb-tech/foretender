# Decisions ledger

Append-only. The adjudicator writes here; critics read it before raising a finding.

This is the only file in `.harness/` that persists across runs, and it's the reason round 3 doesn't
relitigate round 1 — and run 20 doesn't relitigate run 1. **Commit it.**

Prune it occasionally: the value is in the recent, still-relevant entries. An entry whose code no
longer exists is just noise.

Format:

```markdown
## Round <N> — <YYYY-MM-DD>
- **<FINDING-ID> — <title>** — DROPPED (refuted): <reason> — do not re-raise without <what would change it>
- **<FINDING-ID> — <title>** — DEFERRED: out of scope per brief §<x> — tracked in <where>
- **<ID-A> vs <ID-B>** — RESOLVED in favor of <ID-A>: <reason>
- **<decision>** — ACCEPTED: <the deliberate choice, and why it looks wrong but isn't>
```

---

## Phase 0 review — 2026-08-12
- **Phase 0 findings** — ACCEPTED (conditional pass by Henry): nine review conditions folded into PHASE0_FINDINGS.md, KICKOFF.md, CLAUDE.md, PHASE1_INVARIANTS.md, and FTS_DEFECT_REPORT.md; all closed same day, reconciliations verified against the raw corpus.
- **Runtime: TypeScript on Node.js (LTS), strict** — ACCEPTED: Henry's call at review, per the findings' recommendation. KICKOFF §1.4 satisfied — not resolved silently.
- **Data-fact precedence: PHASE0_FINDINGS.md > KICKOFF §2 > BUILD_BRIEF §4** — ACCEPTED: set by Henry at review.
- **OD-1: prediction population (noticeType-segmented vs UK3-only)** — DEFERRED: named open decision for the Phase 1 plan; do not resolve silently. Input: 29-ocid multi-type progression cohort (0/29 in-window conversions).
- **KICKOFF Step 4 "one UTC day" → "one Europe/London day"** — ACCEPTED: corrected per proven window semantics (api-mechanics.md; FTS windows are Europe/London local).
- **Do not build predictions on bids.statistics** — ACCEPTED: 12.2% of Act-regime award volume and falling; annotation added to findings.

## Harness faults — 2026-08-12 (validation run, KICKOFF §4 log)
- **SubagentStop artifact-gate did not fire for a background-spawned planner** — BYPASSED: phase advanced brief→plan-review by hand after verifying .harness/plan.md on disk. Hypothesis: SubagentStop hooks don't run for async/background Agent spawns. Next agent spawned foreground to test. Harness fix batched, not interleaved (KICKOFF §4).
- **Auto-advance also absent on foreground plan-critic spawn** — BYPASSED: gate fired neither for background nor foreground Agent spawns; orchestrator drives state.json phase transitions by hand for the remainder of this run. Same batched-fix item.
- **Plan APPROVED round 2 with 1 MAJOR outstanding (M1: runId/at/epochMs untested)** — ACCEPTED: critic ruled it non-blocking with a two-assertion fix; delegated to test-author as binding addendum instead of a third plan round. N1 (frozen store snapshot is load-bearing) recorded for the simplicity critic.

## Round 1 adjudication — 2026-08-12 (Phase 1 slice 1, review round 1)
- **Verdicts applied: 9 findings verified, 9 CONFIRMED, 0 refuted, 0 uncertain.** I-M1 escalated by its verifier to BLOCKING (§5.3 replay can only replay the first run of a multi-run store). All 25 MINOR findings are unverified (no verdict file) and therefore capped at MINOR; none blocks. Outcome: REVISE, 13 worklist items covering 27 findings.
- **C-m3 vs S-M1/S-M3** — MERGED into S-M1 (links.next type + origin) and S-M3 (walk bounds): same code at `ingest.ts:199`, same fixes. C-m3 is an alias, not extra work.
- **C-m5 vs I-M1** — MERGED into I-M1: both are "replay cannot select a run". C-m5's second half (the divergence error mis-reports the cause as data corruption) is carried into I-M1's fix direction as an error-message requirement.
- **C-m6 vs S-M2** — MERGED into S-M2: identical (`Retry-After` has no ceiling). S-M2's verified TIMEOUT_MAX→1 ms clamp is the sharper half and is what the fix must close.
- **I-m6 vs C-M5** — MERGED into C-M5: same root (`window.ts` validates shape, not calendar). I-m6's live-seam cost (a wasted request against a ~12/120 s budget + an undeletable `run-end {ok:false}`) is carried into C-M5's failure case.
- **S-m1 (redirect: follow) folded into S-M1** — RESOLVED in favour of the one-line `redirect: 'manual'` over widening `TransportResponse` with `response.url`: a 30x otherwise bypasses S-M1's origin pin *after* the check, but three transports and their tests implement that response shape, so changing the seam is out of proportion to the fix. A 3xx now falls through the existing `status !== 200` path and fails loudly, journaled.
- **S-m4 (deep-nesting JSON.stringify RangeError) — IN SCOPE, folded into C-M4** — judged, not deferred: the crash half is already fixed by C-M2's run-end bracketing, and the residual (a record that is neither quarantined nor recorded) contradicts brief req 6's "nothing is silently discarded". The fix is a try/catch plus a bodyHash-reference fallback at the exact call site C-M4 rewrites.
- **S-m5, S-m6, Q-m2, C-m1, S-m2, Q-m4, Q-m3 — FOLDED** into the major items whose fixes touch the same lines. Rule applied: fold a cheap minor only where a major fix already opens that code; otherwise defer.
- **S-m7 — 4.7 MB of real notices (~150 procurement-officer emails, 31 phone numbers) in a PUBLIC repo** — DEFERRED to Henry, deliberately NOT given to the reviser. It is a decision, not a defect: OGL-licensed public-register data, so not a leak, but a conscious republication of personal contact data outside its original context. The reviser must not delete or redact fixtures — brief req 7 requires them committed whole and unedited and the contract tests depend on the exact bytes. Options for Henry before /ship: accept + note it in `test/fixtures/README.md`; make the repo private; or re-scope the fixture set (which reopens brief req 7 and the test-plan, i.e. a planning decision).
- **S-m3 — store `existsSync`/`wx` TOCTOU** — DEFERRED to backlog: the `wx` flag already defeats the symlink case; the residual needs local write access on a single-operator machine and the concurrency half presupposes scheduling, which the brief puts out of scope. Do not re-raise without a multi-writer store or a shared host.
- **S-m8 — no header allowlist on the append-only journal** — DEFERRED to backlog with a hard trigger: do it before any authenticated or cookie-bearing request is ever added, because the store has no rewrite path. Speculative while the only request header is the public UA.
- **C-m4 — one torn NDJSON line bricks the store** — DEFERRED to backlog (store durability / projection-rebuild): the right semantics is a design question the plan never took, and silently skipping a torn line loses a release id and re-accepts it. The failure is loud today, and the projections are rebuildable-from-journal by design. Do not re-raise as a revision item; it needs a rebuild path.
- **I-m3 — `checksums.sha256` has no automated consumer** — DEFERRED to backlog (fold into CI at /ship): additive test surface the plan did not scope; byte-identity was verified this round and the contract tests pin the load-bearing fixture facts incidentally.
- **Q-m1, Q-m5 — test-helper tidying** — DEFERRED: `test/helpers/fixture-transport.ts` and `support.ts` are not touched by any fix this round; an unrelated diff in a helper five test files depend on is not worth a reader-clarity win. Do next time those files are edited.
- **Stray `test/replay-multi-run.test.ts` (written by the I-M1 verifier, owned by no plan step)** — ADOPTED as the I-M1 regression test, to be rewritten in place. Its 5 tests currently pass *by asserting the bug* ("replaying run B diverges at exchange 1", "run A succeeds silently"), so the suite is green while encoding the defect. Rewrite inverts them (run B must replay correctly after the fix), keeps the two-run store fixture, and the file gets recorded against plan step 5's replay bullet so it stops being unowned. Deleting it was rejected: it already builds exactly the store state the fix must handle.
- **Test edits authorised for items 3, 7, 8, 10, 11 and the item-1 rewrite** — ACCEPTED as deliberate behaviour change, not weakened tests: C-M3 changes `lastSeenRelease` semantics, C-M4 changes quarantine idempotency, C-m2 changes counter reconciliation, C-M1 adds a cross-run sleep. Each requires an added or updated assertion, named per item in the worklist. Nothing may be relaxed to go green.
- **Plan defects found by review, to be corrected with the code** — ACCEPTED: (1) plan:68 asserts normatively that "quarantine is stable across re-runs by construction" — C-M4 proves this false at dedupe case 1; (2) plan:195-197's jq pacing check groups by `runId` and is therefore structurally blind to the cross-run violation C-M1 found — it returns 13 for exactly the journal containing a 1 s gap; (3) the plan never specified run selection for §5.3 replay, which is how I-M1 shipped. Each has a bounded local fix, so this is REVISE, not REPLAN — recorded here so round 2 does not re-derive them.
- **Adjudicator agent reported "failed" (session limit) but artifact is complete** — NOT a harness fault: worklist.md (396 lines, valid VERDICT REVISE/13/blocking 1) and this ledger append both landed before the limit hit; the failure was in the final moment after the substantive write. No rework needed.

## Round 2 re-review — 2026-08-13 (Phase 1 slice 1)
- **Revise loop converged in one round.** Round-2 panel (correctness, security, integration — simplicity skipped: 0 majors round 1, guards-added diff) all APPROVED 0/0/0. All 9 round-1 majors verified genuinely closed by executing the round-1 failing cases against the rebuilt dist/ (correctness 36/36 closure assertions; security bypass-tested the origin/Retry-After/walk-bounds guards; integration reproduced the I-M1 two-run replay fix). Zero regressions, zero new findings. No adjudication/verification needed — nothing to verify. Advanced to QA.
- **Fixture PII (S-m7): Henry's decision = accept + note in test/fixtures/README.md.** Repo stays public. README note added at finish (does not touch byte-exact fixtures).

## Harness fix — 2026-08-13 (SubagentStop auto-advance)
- **Root cause CONFIRMED (was hypothesis):** SubagentStop does NOT fire for Agent-tool (Task) subagent spawns. Verified empirically — a capture placed at the very top of `artifact-gate.sh` (before any early-exit; wired from session start so no settings-reload confound; matcher includes `scout`) never ran for a trivially-instructed `scout` Agent-tool spawn. claude-code-guide confirmed the field name (`agent_type`) and matcher semantics were correct and that the docs *claim* the event should fire — so this is a platform limitation the docs don't reflect, not a misconfiguration. The `SubagentStop` hook therefore cannot drive the build-pipeline, which spawns every agent via the Agent tool.
- **Fix:** Extracted the validate+advance logic out of `artifact-gate.sh` into shared functions in `hooks/lib/harness.sh` (`harness_artifact_reason`, `harness_next_phase`, `harness_do_advance`, `harness_panel_complete`, `harness_rostered`). New `bin/advance-phase.sh <agent_type>` runs that same logic on demand; the orchestrator calls it after each agent (exit 0 = advanced/held, exit 3 = artifact missing → re-dispatch). `artifact-gate.sh` refactored to a thin caller of the same functions — retained for the `claude --agent` CLI path and defence in depth. SKILL.md phase-machine section rewritten to instruct the orchestrator to call `advance-phase.sh` and to document the finding. Both callers share one code path, so they can't drift.
- **Tested:** 13 advance-phase branches + 5 hook-payload cases in a sandbox `.harness/` (all pass: happy paths, missing-artifact/missing-verdict → exit 3, panel barrier holds at 2/4 & releases at 4/4, reviser round-bump + verdict-clear, qa PASS→integrate / FAIL→revise, finding-verifier no-move, inactive → exit 2). Real `.harness/state.json` untouched throughout.
- **Supersedes** the two BYPASSED entries above (brief→plan-review and foreground-critic hand-advances): the by-hand driving was the correct interim per KICKOFF §4; this is the batched permanent fix. Next pipeline run should use `advance-phase.sh` and will exercise it for real.

## Round 1 adjudication — 2026-08-13 (Phase 1 slice 2, review round 1)
- **Clean panel, small worklist.** correctness/integration/security all APPROVED 0/0/0; the worst-case false-day-completion was verified structurally impossible (`ingestWindow` resolves only via `return writeRunEnd(true)` on normal `links`-absent termination; every other exit throws and skips `markDayComplete`). simplicity raised 1 MAJOR + 1 MINOR, both DRY, no bugs. Outcome: REVISE, 2 worklist items, 0 blocking.
- **Q-M1 — `loadBackfillDay` re-implements `wholeStreamChain` verbatim** — WORKLISTED (MAJOR, verified CONFIRMED). Not deferred despite being test-only. Reasoning: it is *newly-introduced, in-diff* duplication (the diff created `loadBackfillDay` as a line-for-line copy of the existing chaining body + an identical type + a path-only-differing loader), which is squarely the simplicity lens's remit on a diff review. Round 1 DEFERRED tidying of these exact helper files with the explicit condition "do next time those files are edited" — the files ARE edited now, so the deferral's own trigger is met. Distinct from Round 1's Q-m1/Q-m5, which were pre-existing reader-clarity nits in untouched files. Fix is a behaviour-neutral extract (`chainPages`/`loadFixturePage`/`RoutableChain`); reviser must keep all 93 tests green — a test-helper 7 test files depend on, not a test change.
- **Q-m1 — live CLI transport duplicated across the two `fetch` sites** — WORKLISTED (MINOR, unverified/no verdict → below the MAJOR cap). Not deferred. Reasoning: `USER_AGENT`/`liveTransport`/`sleep` are copied verbatim into the new `backfill.ts` including the security-load-bearing `redirect: 'manual'` (slice-1 S-m1) option and comment. These are the only two `fetch` sites; drift on the redirect option would re-open the S-m1 origin-pin bypass on one CLI. Cheap to close (extract `src/cli/live-deps.ts`) while the new CLI is fresh, and the security-sensitive duplicated decision is what tips a marginal DRY call to "share it."
- **Correctness NOTE (torn `checkpoints.ndjson` line)** — CONFIRMED out of worklist: deferred-C-m4 territory. Backfill inherits the loud-failure behaviour without worsening the dangerous direction (throws; cannot silently skip or fabricate a false-complete day). Per Round 1, C-m4 is "do not re-raise as a revision item; it needs a rebuild path." Not a finding.
- **Correctness NOTE (`markDayComplete` outside `runBackfill`'s try/catch)** — CONFIRMED out of worklist: judged benign. No realistic input makes it throw (a pre-existing torn checkpoint throws earlier at `completedDays()`); no data loss if it did (releases already persisted, un-marked day re-runs idempotently); only concurrent-writer/disk-full trigger it, both out of scope. Not a finding.

## Round 2 re-review — 2026-08-13 (slice 2, behaviour-neutral DRY refactor)
- **Panel fully clean: correctness/security/simplicity 0/0/0, integration 0/0/2 (minor, doc-only).** Both DRY extractions verified behaviour-neutral: live-deps.ts's liveTransport is byte-identical to the slice-1 original modulo `export` (correctness diffed it), redirect:'manual' S-m1 safeguard intact and now single-sourced (security grep-confirmed), duplication cleanly resolved with no third copy (simplicity), wiring clean no cycle (integration). 93/93 green.
- **No BLOCKING/MAJOR findings → no verify/adjudicate step; SHIP.** Advanced review→adjudicate→qa; the empty adjudicate is a no-op for a zero-finding panel.
- **2 doc-only MINORs deferred to integrate:** fetch-day.ts:4 and CLAUDE.md:82 still claim fetch-day is "the ONLY file that references global fetch" — stale after the extraction moved fetch into live-deps.ts. Integrator to correct (behaviour-neutral doc fix; squarely scar-tissue).

## Harness naming bug — 2026-08-13 (batched, not interleaved per KICKOFF §4)
- **artifact-gate / harness_artifact_path expects `integration-log.md`; the integrator agent writes `integration-report.md`** (both slices). advance-phase.sh correctly exited 3 (gate working — expected artifact absent); hand-advanced integrate→done after verifying the integrator's real artifact (.harness/integration-report.md), BEHAVIOR CHANGES 0, 93/93 green. FIX (batch): reconcile the name — either point harness_artifact_path (hooks/lib/harness.sh) + artifact-gate at integration-report.md, or change the integrator agent to write integration-log.md; then upstream to ~/.claude/skills. One-line either way; not urgent (integrate is the penultimate phase and its output isn't a downstream input).

## Forward commitments — 2026-08-13 (set by Henry)
- **Overnight gate OPENED** (~/.claude/autonomous/.overnight-enabled armed) after two clean supervised runs. First unattended run = the harness integrator-artifact-name fix, as a filename-blast-radius shakedown of the overnight path (queued, running).
- **Build-order lesson:** slice-2's zero bugs vs slice-1's nine reflects slice 2 riding a hardened core, not being better-written. Front-load hard primitives; keep later slices thin. Normalisation is the real test of the pattern (it can't ride slice-1's core).
- **NEXT SLICE = normalisation / canonical model, overnight.** Two conditions MUST be in its plan before it starts (Henry):
  1. **Contract tests assert FIELD-PRESENCE RATES against the saved corpus fixtures, matched to the Phase 0 census** — not "parses without error". Minimums: tender.value.amountGross ~99.1% (core amount ~81.7%); CPV via tender.items[].additionalClassifications 100%, with items[].classification AND tender-level contractPeriod asserted at ZERO occurrences; tender.lots[].contractPeriod ~99.1%; submission deadline (tenderPeriod.endDate ∪ expressionOfInterestDeadline) ~99.2%; mainProcurementCategory 100%. A wrong field path must surface as a coverage number disagreeing with the census, not as an absence of errors.
  2. **FAIL LOUDLY ON UNMAPPED SHAPES** (BUILD_BRIEF §5.4): an unrecognised regime/notice-type/field combination records an anomaly and surfaces it — never silently defaults or coerces. Normalisation is the first brick whose bugs are QUIET (a wrong path yields a clean-looking but wrong canonical model that every upper layer inherits). Anomaly rate is a health metric.
  Branch on tender.legalBasis.scheme throughout; hold to PHASE1_INVARIANTS.

## Tracked batched harness items — 2026-08-13
- **UPSTREAM the integrator artifact-name tolerance to the global template.** After Henry merges foretender PR #1 (the repo-local fix in .claude/hooks/lib/harness.sh — accept integration-log.md OR integration-report.md), port the same `integrator)` branch into ~/.claude/skills/scaffold-harness/templates/hooks/lib/harness.sh so future scaffolded repos get it. Same follow-up pattern as the SubagentStop fix upstream. TRACKED, not yet done (gated on the merge).
- **CLASS fix, not just this instance (Henry's sharper lesson):** the drift was orchestrator prompts restating an artifact name instead of sourcing it from the agent definition. Filename tolerance patches the gate; the class is "prompts restate what the agent contract already fixes." Discipline going forward: dispatch prompts source artifact/verdict/phase names from .claude/agents/<agent>.md, never from recall. Recorded in memory [[orchestrator-source-artifact-names-from-agent-defs]]. No code change owns this — it's an orchestration-discipline item; the tolerance fix is the only code part.

## Round 1 plan — 2026-08-13 (slice 3, factual correction)
- **Fixture CELEX count: scout.md and the orchestrator's ground-truth were WRONG (0 CELEX); correct is 204 UKPGA + 26 CELEX = 230 unique tender-tagged in committed fixtures.** The planner verified from disk and flagged the contradiction rather than following the orchestrator's wrong instruction — the pipeline's verify-don't-recall discipline catching an orchestrator error. Both regimes testable from the committed corpus; the Phase 0 CELEX-rich-page commit is dropped (planner's optional Step 11, not needed). scout.md annotated with the correction; plan.md is authoritative for fixture counts.
- **m2 ORCHESTRATOR RULING: full-rebuild projection APPROVED (not append-only).** The canonical projection is a DERIVED view of the append-only raw store, not a source of truth. Rebuilding it deterministically from the raw store each run is idempotent + bit-for-bit reproducible (§5.3); append-only would risk staleness/duplication. Append-only lives where it must — the raw store + ledger (slice 1). Brief req 6 delegated projection form to the planner; this is within that. Do not re-raise.
- **m1: CELEX lot branch must read contractPeriod.endDate** (for pinned lotEndDate 5) — build note for the builder; coverage test backstops it.
- **Condition-1 teeth (plan-critic NOTE):** census-100% required-field coverage over canonical records is tautological; the wrong-path signal must come from the NULLABLE-field rates + independent zero-guards (test-side structural reader over raw bodies) + the anomaly count. Test-author: field-presence tests compare canonical output against an INDEPENDENT structural read of the raw fixture bodies, so a wrong path in the normaliser produces a canonical-vs-raw disagreement (not a tautology).

## Round 1 adjudication — 2026-08-13 (Phase 1 slice 3, review round 1)
- **Verdicts applied: panel APPROVED — correctness 0/0/0, integration 0/0/2, security 0/0/2, simplicity 0/0/3. No verdict files → every finding UNVERIFIED, capped at MAJOR; all are MINOR, none blocks.** Correctness executed the compiled normaliser over all 230 tender-tagged fixture releases with 0 per-release field-mapping mismatches against an independent raw-path read — no bug to fix. The stale `qa-report.md` on disk is slice 2 (`PASS_WITH_ISSUES`, commit `f1f2d95`), NOT a slice-3 QA failure; it does not force REVISE. Outcome: REVISE, 3 worklist items (4 findings), 0 blocking.
- **I-m1 vs Q-m2 — MERGED:** identical finding (the third `releases.ndjson` path literal at `raw-store.ts:353`, also at `:171` and `support.ts:78`). One entry, both IDs aliased.
- **I-m1 / Q-m2 (merged) — DEFERRED to backlog:** drift risk, not a break (both product literals byte-identical today). Extracting a helper and using it at `:171` touches an existing line, violating the strictly-additive build contract (build-log deviation 1). The panel has twice deferred this exact path-literal class (Round-1 Q-m4; `support.ts` dup) and both lenses that raised it concur it is deferrable. Do next time `raw-store.ts` is opened for another reason. Do not re-raise without an actual rename of `releases.ndjson` or a third product reader.
- **S-m2 — DEFERRED to backlog (defense-in-depth):** stderr echo of untrusted `id`/`bodyHash` (`project.ts:85`, `raw-store.ts:372`) fires only on an already-tampered/mismatched store (attacker already holds local write); single-operator terminal, no log aggregation; not co-located with any folded fix. Do not re-raise without a log-aggregation sink or a path that echoes untrusted data on a normal (non-tampered) run.
- **S-m1 + Q-m3 — RESOLVED (co-located fold):** Q-m3 (unreachable `?? 'CPV'` at `normalise.ts:51`) is folded INTO the S-m1 fix because it lives in the same three-line `toCpv` function S-m1 edits (`normalise.ts:52`) — the "defer unless trivially co-located with a folded fix" exception is met. S-m1 worklisted on substance: a single hostile/deep CPV `id` (`String(deep-array)` `RangeError`) aborts the whole-store projection; a guarded coercion keeps the fail-loud property (nullable id, `missing-cpv` anomaly intact) without letting one release kill the run. This is the one item that changes an error path — a unit test for the guarded path is authorised. Behaviour-neutral over all 230 fixtures (ids are strings); keep 123 tests green.
- **I-m2 — WORKLISTED:** CLAUDE.md "Running it"/"Layout" omit the new `normalise.js [--store DIR]` CLI and `src/normalise/` module — the doc the harness gate and a human read. Cheap doc touch, behaviour-neutral.
- **Q-m1 — WORKLISTED:** `hasOptions`/`optionsDescription` (`model.ts:51-52`, `normalise.ts:98,109-110`) exceed the brief's census set (req 3) with zero consumer this slice — no test, no `fieldCoverage`, no CLI line, no reader of `canonical.ndjson`. Cheap removal.
- **Correctness NOTE (canonical `cpv[]` is a list, not a set) — ACCEPTED, stays OUT:** design-consistent verbatim/union read (build-log deviation 2), duplicate-tolerant in this slice (`cpv.length` / `.some(...)`). It is a future CPV-slicing-slice question (PHASE1_INVARIANTS #16) with no wrong answer now. Do not worklist until that slice decides set-vs-list.

## Round 2 re-review + S-m3 disposition — 2026-08-13 (slice 3)
- **Round-2 panel: correctness/integration/simplicity 0/0/0, security 0/0/1.** All three round-1 worklist items verified clean; correctness confirmed the CPV-guard behaviour-neutral (all 1415 real CPV ids are strings; guard only diverges on the 0-occurrence pathological case). S-m1 verified genuinely closed against the compiled dist.
- **NEW finding S-m3 (security, MINOR): sibling of S-m1.** An identical unguarded `String(scheme)` at src/normalise/normalise.ts:148 (unknown-regime branch) still aborts the whole projection on a 40k-deep hostile scheme. Pre-existing (untouched by the revision), same class as the just-fixed S-m1.
- **ORCHESTRATOR DECISION: close the class in a targeted round-3 revise** (not defer). Rationale: it's the same class the reviser just fixed; the S-m1 fix's stated property ("one hostile release can't abort the run") is not honest while the twin is unguarded; cheap. Fix = generalize the coercion guard (a shared safe-string-coerce used at BOTH the CPV-id site and the scheme site), so the class is closed not just two instances. No full adjudicator run for one obvious same-class MINOR — disposition recorded here. Deferred: S-m2 (stderr echo), the duplicated releases.ndjson literal — unchanged.
- **Round-3 re-verify done INLINE by the orchestrator, not a full round-4 panel** (round budget = 3; a full re-panel would exceed it and be disproportionate for a rename+guard). Independently reproduced against rebuilt dist: S-m3 (40k-deep scheme) → unknown-regime anomaly, NO THROW; S-m1 (40k-deep CPV id) → no throw (non-regression); real-store projection UNCHANGED (218/196/22, 0 anomalies) = behaviour-neutral. All four review dimensions covered: correctness (behaviour-neutral, real-store identical), security (S-m3 closed = the finding's own lens satisfied), integration (internal rename of a non-exported helper, no interface change — makeAnomaly already accepts widened types), simplicity (one shared safeStr replaces two guards, strictly simpler). Class of "unguarded String() coercion aborts the run" is CLOSED. 125/125 green.

## Round 1 scout — 2026-08-13 (slice 4, factual correction)
- **Fixture multi-notice count: scout said 18 (whole-stream only); AUTHORITATIVE is 84 across ALL committed fixtures** (1624 releases / 1492 ocids; scout missed the backfill set's 16 pages). Same undercount pattern as slice-3 (scout missed CELEX). scout.md annotated with the authoritative count + concrete example ocids per lifecycle/dirty-reality sequence; the projection/tests must dedup by release id across fixture sets (291 ocids overlap whole-stream∩backfill via the 08-10..12 window). scout's other findings stand.

## Plan-critic BYPASS — 2026-08-13 (slice 4, infrastructure failure)
- **The plan-critic subagent failed TWICE on infrastructure (not harness/plan): first "computer went to sleep mid-response", then a 600s stream-stall watchdog.** Both left the STALE slice-3 plan-critique.md on disk (valid-looking APPROVED verdict but reviewing the wrong slice) — caught by READING the artifact, not trusting the verdict. Per KICKOFF §4 (sustained non-harness faults → complete the step by hand, log it), the orchestrator wrote the lifecycle plan-critique.md by hand, with INDEPENDENT recomputation: reimplemented the plan's Step-3 fold rules in Python over all committed fixtures and reproduced EVERY pinned number (1624/1492; state dist 1016/278/166/20/12; 5 anomaly events/4 ocids; reasons OoO 3/contradiction 2; ocids 05495a/05faa0/06d2d1/06e072; orphan 125; skipped 2). Verdict APPROVED, 0 blocking/0 major, 2 minor. Rulings: export safeStr (not mirror); full-rebuild projection (as slice-3). The by-hand critique kept the same rigor (numbers verified against disk) as the agent would have; independence preserved (orchestrator did not write the plan).

## Round 1 adjudication — 2026-08-13 (Phase 1 slice 4, review round 1)
- **Verdicts applied: 5 findings raised, 1 verified (C-B1 CONFIRMED at BLOCKING by orchestrator independent recompute), 0 refuted, 0 uncertain. The other 4 (C-m1, I-m1, Q-m1, Q-m2) have no verdict file → unverified, capped at MAJOR; all are MINOR, none blocks.** Security clean (0/0/0). Outcome: REVISE, 5 worklist items, 1 blocking.
- **C-B1 — combined `planning,tender` release mis-staged `pipeline`** — WORKLISTED (BLOCKING). The plan's Step-3 fold spec ("first match wins" over the RULES table, `planning` listed before `tender`) stages a single-release active tender (`tag:["planning","tender"]`) by its LOWEST-listed tag, contradicting the module's own "monotonic high-water stage" model. Confirmed by independent recompute: exactly 2 ocids (06e193/06e0ec) move pipeline→tender; slice-3's shipped normaliser canonicalises the same 2 as tenders (230 = 228 + 2). This is the "reproduces the aggregate while wrong on an edge" case, NOT a settled decision — decisions.md had no ruling on how a multi-family combined tag stages, and the plan-critic's Python recompute shared the same first-match rule so its `pipeline:166` pin ENCODED the defect. FIX = classify a non-terminal release by MAX rank over its stage tags (terminal precedence + required-stage check unchanged; same-rank stage-vs-amend tie-break specified so classify stays total). Corrected pins pipeline 164 / tender 280 (rest UNCHANGED). Plan text corrected at plan.md:106/127/141 and the Step-3 prose alongside the code — a bounded local plan-defect correction, not REPLAN.
- **Test-pin edit AUTHORISED (deliberate correction, not weakened test):** `test/lifecycle.contract.test.ts:186-187` pipeline 166→164, tender 278→280. The old integers pinned the bug; the reviser flips exactly these two and nothing may be relaxed to go green. All 152 tests stay green after.
- **Q-m1 (dead `label` field, 12 rows) + Q-m2 (dead `?? events[0]?.ocid` fallback) + C-m1 (`reconstructOne` docstring over-claims permutation-invariance)** — FOLDED into C-B1's machine.ts pass. Rule applied: fold a cheap minor only where the blocking fix already opens that code — all three live in the same file/function C-B1 rewrites. C-m1 is DOCSTRING-ONLY (state the releaseId-uniqueness precondition the module doc already conditions on); NO `sortEvents` content tiebreak, which would risk the pinned counts and duplicate semantics. C-m1's under-corpus generalization (`["tender","award"] → awarded`) is closed as a class by C-B1's max-rank fix — correct, not a regression.
- **I-m1 (CLAUDE.md omits the lifecycle CLI / `src/lifecycle/` layer)** — WORKLISTED (MINOR). Cheap behaviour-neutral doc touch; the panel worklisted the analogous slice-3 gap (I-m2) so this follows precedent, not creep.
- **Projection read-spine duplication (simplicity NOTE)** — DEFERRED to backlog: same class twice deferred (slice-3 I-m1/Q-m2, slice-1 Q-m4); extraction edits shipped `normalise/project.ts` + `raw-store.ts` against the additive contract; both literals behaviour-identical today. The simplicity lens itself ruled "do not worklist." Do not re-raise until `raw-store.ts` or either projection is opened for another reason, or a third projection reader appears.
- **`project.ts:60` tampered-store id/bodyHash stderr echo (security NOTE)** — DEFERRED: new instance of slice-3 **S-m2** (already deferred), inherits that disposition — fires only on an already-tampered single-operator store, no log sink, not co-located with a folded fix. Do not re-raise without a log-aggregation sink or a normal-run untrusted-echo path.
- **SETTLED, not reopened:** `safeStr` export-vs-mirror (export, per plan-critic ruling); full-rebuild projection; `noticeType` as a brief req-1-mandated event field (not slice-3 `hasOptions`-style speculation — simplicity confirmed the distinction).

## Round 2 re-review — 2026-08-13 (slice 4, C-B1 fix)
- **Panel fully clean: all four lenses APPROVED 0/0/0.** C-B1 verified closed AND fully by an independent from-scratch max-rank recompute reproducing the shipped distribution ocid-for-ocid (164/280/1016/20/12); tag census confirms planning,tender is the ONLY cross-rank multi-family combo (no other case the rule could mishandle); terminal precedence + stage-beats-amend tie-break demonstrated total + order-independent; label/fallback removals behaviour-neutral. 154/154. Revise loop converged in one round.
- **No BLOCKING/MAJOR → no verify/adjudicate; SHIP.** Hand-advanced review→adjudicate→qa. C-B1 was the panel catching a spec bug that both the plan-critic (orchestrator, by-hand) and test-author had reproduced as "correct" — the aggregate-reproducing verification was necessary but not sufficient; only the correctness lens's cross-check against slice-3 canonicalisation caught it. The strongest validation this run of the supervised-panel discipline on a semantic slice.

## OD-1 RESOLVED — 2026-08-13 (Henry, at the slice-5 brief gate)
- **OD-1 (prediction population): SEGMENT BY noticeType.** One pipeline-to-tender prediction type, predicting UK1/UK2/UK3 with per-noticeType priors and per-type calibration tracks (implements §6's segmented calibration; uses all signal; UK3 foregrounded in reporting). NOT UK3-only. Parked since the Phase 0 review; now live because the prediction slice is being built. Slice-5 brief approved same gate.

## Round 1 plan-critique — 2026-08-13 (slice 5): B1 + M1 rulings
- **B1 (BLOCKING, plan-critic, CONFIRMED sharp): --asof parsed in runner TZ → scoreboard non-deterministic across environments** (UTC CI vs Europe/London Mac differ by 1h; the same-machine determinism test hides it). FIX: --asof MUST carry an explicit offset (Z or ±HH:MM), validated (reject the bare 19-char form); toEpochMs then TZ-independent. Stored notice dates already carry +01:00, so only --asof needed the fix.
- **M1 (MAJOR) ORCHESTRATOR RULING: full-rebuild derived view is CORRECT for this batch slice.** The "predictions stated in advance" honesty is enforced by the DATE-CUTOFF (predictor sees only events ≤ madeAt), NOT by append-only storage — so a deterministic full-rebuild (slice-3/4 precedent) is honest and correct. The plan must STOP calling the ledgers "append-only"; state they are deterministic rebuildable views of (store + priors + asof). Verdicts are inherently a derived-at-asof view (a prediction's verdict changes pending→resolved as asof advances). **Genuine cross-run append-only prediction accumulation is DEFERRED to Phase 2 (self-scheduled wake), where predictions accumulate over real time.** The brief's "append-only" wording for these ledgers was an orchestrator over-specification — SUPERSEDED by this ruling and amended in the brief.
- m1: cutoff/noticeType needs a total tie-break (by sortEvents/releaseId; noticeType = the event AT the cutoff epoch). m2: define the None bucket precisely (surface a non-UK1/2/3 noticeType rather than silently folding into null-None, per pipeline-volume.md "unknown values expected"). NOTES: Step 11 assert only ≥1 REAL resolved (mandated); relax "all three verdicts" to what the corpus yields.

## Round 1 plan revision — 2026-08-13 (slice 5, planner): how B1/M1/m1/m2 landed in plan.md
- **B1 → FIXED.** Added `requireOffsetIso` + `ISO_WITH_OFFSET` to `src/lifecycle/date.ts` (plan Step 2); `--asof` must carry an explicit offset (Z or ±HH:MM), bare 19-char form REJECTED with a clear error, validated at the CLI (Step 8) and each projection entry (Steps 4, 8). `toEpochMs` is offset-aware/TZ-independent. New TZ-independence test (Step 11): projections run under TZ=UTC and TZ=America/Los_Angeles must produce byte-identical predictions.ndjson/verdicts.ndjson/scoreboard.json — the test that would have caught B1 — plus a `requireOffsetIso` rejection unit (Step 10). Empirically verified Node re-reads process.env.TZ per Date op, so the in-process two-zone test is sound. All test asof literals now offset-carrying. Decided NOT to edit test/helpers/support.ts's ISO_WITH_OFFSET copy (production must not import from test/; shared-helper edit for a 1-line DRY win is the panel's repeatedly-deferred class) — the two one-line copies are recorded as deliberate in Interface changes so simplicity does not re-raise.
- **M1 → FIXED (wording).** Ledgers no longer called "append-only" anywhere (Goal/Scope/Approach/Steps 4,8/Interface/Risks). Stated as DETERMINISTIC REBUILDABLE VIEWS of (store + priors + asof), full-rebuilt each run (slice-3 m2 / slice-4 precedent), honouring §5.3; append-only guarantee lives in the raw store; "stated in advance" honesty attributed to the DATE-CUTOFF. Cross-run append-only accumulation DEFERRED to Phase 2 in Scope-Out + Alternatives. Internal self-contradiction resolved.
- **m1 → FIXED.** Step 3 total tie-break: cutoff = min epoch over dated planning-family events; noticeType = the noticeType of the `sortEvents`-first planning event AT the cutoff epoch (handles same-epoch-different-noticeType and null-earliest-date), never using id order to include/exclude an event. Order-independence unit in Step 10.
- **m2 → FIXED.** Chose `None` = null-or-unrecognised noticeType (documented catch bucket); model/calibrator framing now matches the `else → None` implementation. Not silent: raw noticeType preserved verbatim in every record; a Step-10 `noticeType:'UK99'` unit proves an unexpected code is surfaced in its record while its calibration folds into `None` deliberately (per pipeline-volume.md "unknown values expected"). Picked the documented-catch option over a dynamic per-code segment map to keep scoreboard.json's fixed-key byte-determinism; flagged the alternative in Open questions.
- **NOTES → APPLIED.** Step 11 hard-asserts ONLY ≥1 real resolved (069311 converted witness) + structural completeness; "all three real verdict classes" relaxed to logged counts asserted at the level the corpus provably yields (Risks row 7). Added the one-line `tenderUpdate`-as-onset comment on `TENDER_ONSET_TAGS` (Step 5), referenced in Step 6.

## Round 2 plan-critique — 2026-08-13 (slice 5): APPROVED
- Round-2 plan-critic (fresh, overwrote round-1 critique): **APPROVED, 0 blocking / 0 major / 1 minor**. All four round-1 findings verified genuinely resolved against code + on-disk fixtures (not reworded): B1 (--asof offset-required at all 3 entry points; every other epoch-compared date confirmed offset-carrying on disk), M1 (no surviving append-only ledger claim), m1 (total tie-break, null-dated pre-filtered, no leakage regression), m2 (None=null-or-unrecognised consistent, UK99 unit). Both residual risks confirmed: Node re-reads process.env.TZ per Date op (TZ-flip test is sound, no child-process fallback needed); no date the epoch-compare touches is bare 19-char.
- **Round-2 MINOR (carry into build, non-blocking):** the TZ-flip regression test could silently false-green if it ever stops exercising two zones. HARDENING: add a one-line self-verifying probe inside the test (assert the two TZ settings actually produce different bare-parse epochs before asserting offset-parse byte-identity), so the test fails loud if it degenerates. Independently backstopped by the Step-10 requireOffsetIso rejection unit + Step-8 CLI check. Test-author to implement.

## Round 1 adjudication — 2026-08-13 (Phase 1 slice 5, review round 1)
- **Verdicts applied: 6 findings raised, 1 verified (Q-M1 CONFIRMED at MAJOR), 0 refuted, 0 uncertain. The five MINORs (C-m1, I-m1, I-m2, S-m1, Q-m1) have no verdict file → unverified, capped at MAJOR; none reaches the cap.** Panel APPROVED across all four lenses (correctness 0/0/1, integration 0/0/2, security 0/0/1, simplicity 1/0/1). No QA report for this slice. Outcome: REVISE, 2 worklist items covering 4 findings, 0 blocking.
- **Q-M1 — grouped-by-ocid read spine byte-identical in both new projections** — WORKLISTED (MAJOR, verified CONFIRMED). The slice-4 "Projection read-spine duplication" deferral (this ledger, ~line 127) set an explicit re-raise trigger: "or a third projection reader appears." Slice 5 adds the third AND fourth readers (`prediction/project.ts` inline + `grading/project.ts` `readEventsByOcid`), byte-identical to each other — the trigger is met. Decisive difference from every prior deferral of this class (slice-1 Q-m4, slice-3 I-m1/Q-m2, slice-4 read-spine): those all required editing SHIPPED files against the additive contract; here both copies are in NEW slice-5 files and the fix only ADDS `src/lifecycle/read.ts` downward — purely additive, §5.1-safe (verifier confirmed grading never imports prediction; both already import lifecycle). Same profile as slice-2 Q-M1, worklisted then. Do not re-defer: the trigger and the additivity are both settled.
- **Q-m1 — `segmentOf` + `Segment` union duplicated across the two layers** — FOLDED as a rider on Q-M1: same downward home (`lifecycle/read.ts` or a tiny `lifecycle/segment.ts`) hosts both without coupling the layers. Simplicity's own ruling: worth doing only as a rider on Q-M1; if Q-M1 were deferred, leave this too. Q-M1 is fixed-now, so it rides. `priorFor` stays apart from `segmentOf` (different return types/reasons — coincidental, not duplication; simplicity NOTE, recorded so a later round does not fuse them).
- **I-m1 vs S-m1 — MERGED into one worklist item.** Same root seen by two lenses: the `predictions.ndjson` file-as-interface is untyped — integration flagged no shared compile-time key between predictor `Prediction` and grader `PredictionRecord` (a rename silently mass-mis-grades to all-`pending`); security flagged `readPredictionRecords` coercing a malformed-but-valid-JSON row silently (`Number(undefined)→NaN` into the Brier fold) instead of failing loud. Both fixes converge on a fail-loud shape validation in `readPredictionRecords`. WORKLISTED (MINOR): folds on the co-located rule (lives in `grading/project.ts`, which Q-M1 already opens) AND serves this project's load-bearing "FAIL LOUDLY ON UNMAPPED SHAPES" bar (BUILD_BRIEF §5.4; the normalisation-slice forward commitment). Both lenses judged it MINOR and currently test-protected/unreachable, but the standing bar plus the cheap co-located fix tip it to fix-now, not defer.
- **I-m2 — `checksums.sha256` not updated for the two new `conversions/` fixtures** — DEFERRED to /ship. Established pattern: the manifest has no automated consumer (inherits slice-1 I-m3, slice-3). Byte-integrity of the new fixtures is pinned incidentally by the contract test's exact `069311` date/notice assertions. Do not re-raise as a revision item; it folds into CI at /ship.
- **C-m1 — a real null/non-scalar-dated tender onset grades `not_converted` (false negative)** — DEFERRED to backlog. The correctness lens itself marked it UNREACHABLE on the committed corpus (0 of 1708 tender-family releases carry a null/non-scalar `date`; OCDS requires `date`, FTS supplies it offset-carrying). The fix direction ("record an anomaly rather than silently drop") needs an anomaly channel in grading that the plan did not scope — grading emits verdicts + scoreboard, not anomalies — so it is a design decision, not a local patch. Do not re-raise without a real dateless tender-family release in the corpus, or once grading gains an anomaly sink.

## Round 2 re-review — 2026-08-13 (slice 5): panel CLEAN, 2 minors
- Round-2 re-review of the revise delta (read.ts/segment.ts extraction + fail-loud guard): all four lenses APPROVED. correctness 0/0/0 (extraction byte-neutral, verified by execution; guard rejects no legit row — prob 0 passes, null noticeType passes), integration 0/0/0 (§5.1 holds, no cycle, one downward home per symbol, guard accepts every field predict.ts writes), security 0/0/1, simplicity 0/0/1. Zero blocking, zero major.
- Two round-2 minors for adjudication: (S2-m1) requireOffsetIso validates SHAPE not PARSEABILITY — a calendar-invalid but regex-passing --asof (e.g. 2026-13-45T..Z) → Date.parse NaN → silent all-pending scoreboard (a fail-loud residual on Henry's explicit bar; fix = reject isNaN(Date.parse)). (Q2-m1) a consumer-less Segment re-export the extraction added (grading/model.ts, softer prediction/model.ts) — dead weight, fold-or-drop.

## Round 2 adjudication — 2026-08-14 (slice 5): by-hand bypass (session limit)
- The round-2 `adjudicator` subagent (a0688cd) terminated on an ACCOUNT SESSION LIMIT (API error, resets 22:20 Europe/London) having written nothing ("Let me read the inputs before adjudicating"). INFRASTRUCTURE failure, not a work fault.
- Bypass per KICKOFF §4 + slice-4 precedent (by-hand plan-critique when the agent failed on infra): with 0 blocking / 0 major and 2 non-overlapping minors, there is nothing to dedupe/rank/verify — fix-now-vs-defer is an orchestrator judgment. Adjudicated by hand → REVISE (fix both): S2-m1 (requireOffsetIso reject unparseable date — closes the fail-loud gap on Henry's bar) + Q2-m1 (drop dead Segment re-export). Worklist written by hand, marked by_hand:true.
- **PIPELINE PAUSED on the session limit.** All remaining phases (revise round 3, qa, integrate, report) require subagent spawns, which fail until the ~22:20 Europe/London reset. Resume: advance adjudicate→revise, spawn reviser with the by-hand worklist, targeted re-verify (suite+typecheck+build+guard spot-check, NOT a 4th full panel — logged proportionate choice), then qa→integrate→report. Slice acceptance is already fully met (187/187, no-leakage + determinism proven, ≥1 real resolved); round 3 is polish.

## Round 3 revise + targeted re-verify — 2026-08-14 (slice 5)
- Reviser applied both round-2 minors: S2-m1 (requireOffsetIso now rejects Number.isNaN(Date.parse) after the shape check — closes the fail-loud gap; +1 focused test) and Q2-m1 (dropped the consumer-less Segment re-export from grading/model.ts + prediction/model.ts, keeping the internally-used import). 188/188, typecheck+build clean, §5.1 holds.
- **Proportionate bypass of the round-3 re-review panel (logged orchestrator decision):** two full panels already returned clean (rounds 1+2). Round 3's diff is a one-line validation tightening (test-proven) + a typecheck-gated dead-export removal — nothing a 4th full panel could find that a targeted verify doesn't, and a 4th panel would push past the round budget (max 3) for zero signal. Substituted an orchestrator targeted re-verify: independently ran typecheck (clean), full suite (188/188), build (clean), §5.1 grep (clean), AND the S2-m1 fix END-TO-END through the CLI — `--asof 2026-13-45T00:00:00Z` now exits 1 with a clear fail-loud message (was a silent all-pending scoreboard), while a valid `--asof` still resolves the 2 real conversions (overall brier 0.7839). Consistent with the SKILL granting roster judgment per phase.

## QA — 2026-08-14 (slice 5): PASS_WITH_ISSUES, acceptance holds
- QA PASS_WITH_ISSUES: 8/8 brief requirements verified, 0 blocking / 0 major, 2 minor. Acceptance HOLDS — real data/backfill-qa run resolves 2 REAL pipeline→tender conversions (UK1 ocds-h6vhtk-06e21a, UK3 ocds-h6vhtk-06b607); §5.3 determinism byte-identical (sha256 + diff); S2-m1 calendar-invalid --asof fails loud leaving the store unchanged; regression pins normalise 230 / lifecycle 1492·1624·164·280 unchanged; 188/188.
- **D1 + D2 DEFERRED to backlog (orchestrator decision, off the real user path):** D1 empty-store `gradedAt=""` (degenerate no-predictions store); D2 raw ENOENT on a nonexistent --store (still exits non-zero — loud, just an ugly message). Neither affects the real user path or any acceptance criterion; both fail/behave loudly (no silent degrade). Same class as the already-deferred S-m2 tampered-store echo. At round 3 (budget); not worth a round-4 revise for cosmetics on degenerate inputs. Candidate /ship polish alongside the friendly-store-error backlog item.
